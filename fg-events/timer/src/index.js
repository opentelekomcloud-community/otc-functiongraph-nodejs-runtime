"use strict";
const {TimerEvent} = require("./timerevent");

/**
 * Public type exports for editor IntelliSense in downstream JavaScript projects.
 * @typedef {import("./timerevent").TimerEventJSON} TimerEventJSON
 */

module.exports ={
  TimerEvent
}