{
  "name": "dependency-kafka-sample-producer",
  "version": "1.0.0",
  "description": "Sample on how to use Kafka dependency in FunctionGraph",
  "keywords": [
    "FunctionGraph",
    "Dependencies",
    "event",
    "kafka"
  ],
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "postpack": "rm -f ${npm_package_name}.zip && tarball=\"${npm_package_name}-${npm_package_version}.tgz\"; tar -tf $tarball | sed 's/^package\\///' | zip -@r ${npm_package_name}.zip; rm $tarball"
  },
  "devDependencies": {
    "fg-runtime": "file:../../../fg-runtime",
    "kafkajs": "^2.2.4"
  }
  
}
