{
  "name": "scratch-event-timer",
  "version": "1.0.0",
  "description": "A sample for timer event processing with scratch event",
  "main": "src/index.js",
  "type": "commonjs",
  "engines":{
    "node": "20.15.1"
  },
  "cpu": [
    "x64"
  ],
  "os": [
    "linux"
  ],
  "scripts": {
    "postpack": "rm -f package.zip && tarball=$(npm list --depth 0 | sed 's/@/-/g; s/ .*/.tgz/g; 1q;'); tar -tf $tarball | sed 's/^package\\///' | zip -@r package; rm $tarball"
  },
  "devDependencies": {},
  "dependencies": {
    "timer-event": "file:../../fg-events/timer"
  },
  "files": [
    "src/**/*"
  ],
  "bundleDependencies": [
    "timer-event"
  ]
}
